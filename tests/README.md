# Tests

Automated tests and reproducible compatibility checks for ExeBridge belong in this directory.

Useful test targets include prefix creation, launch-profile selection, UMU command construction, desktop shortcut generation, and configuration handling.
